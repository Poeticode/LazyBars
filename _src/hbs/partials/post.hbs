{{> head bodyClass="post"}}
    {{> nav }}  
    <div class="container">
        <div class="row">
            <div class="col c4">
                <h3>{{this.title}}</h3> by {{this.author}}
                <p>{{this.content}}</p>  
            </div>
        </div>
    </div>
{{> foot }}
<!-- Always shows a header, even in smaller screens. -->
<div class="mdl-layout mdl-js-layout mdl-layout--fixed-header">
  <header class="mdl-layout__header">
    <div class="mdl-layout__header-row">
      <!-- Title -->
      <span class="mdl-layout-title">{{ site.title }}</span>
      <!-- Add spacer, to align navigation to the right -->
      <div class="mdl-layout-spacer"></div>
      <!-- Navigation. We hide it in small screens. -->
      <nav class="mdl-navigation mdl-layout--large-screen-only">
            {{#each site.menu }}
            <a class="mdl-navigation__link" href="{{#if this.link }}{{../site.root}}{{this.link}}{{else}}#{{/if}}">{{capitalize this.name}}</a>
            {{/each}}
      </nav>
    </div>
  </header>
  <div class="mdl-layout__drawer">
    <span class="mdl-layout-title">{{ site.title }}</span>
    <nav class="mdl-navigation">

        {{#each site.menu }}
        <a class="mdl-navigation__link" href="{{#if this.link }}{{../site.root}}{{this.link}}{{else}}#{{/if}}">{{capitalize this.name}}</a>
        {{/each}}

    </nav>
  </div>
  <main class="mdl-layout__content">
    <div class="page-content"> <div>{{ package.version }}</div>
        <!-- Your content goes here -->
    
    


{{!-- 
<nav class="nav" tabindex="-1" onclick="this.focus()">
    <div class="container">
        <a class="pagename current" href="{{site.root}}index.html">{{ site.title }}</a>
        {{#each site.menu }}
        <a href="{{#if this.link }}{{../site.root}}{{this.link}}{{else}}#{{/if}}">{{capitalize this.name}}</a>
        {{/each}}
    </div> 
</nav> 
<button class="btn-close btn btn-sm">×</button>
--}}
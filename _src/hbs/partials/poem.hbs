{{> head bodyClass="poem"}}
    {{> nav }}  
<div class="mdl-grid">
    <div class="mdl-cell mdl-cell--4-col center-cell">
        <h3>{{this.title}}</h3>
        <p>{{{this.html_content}}}</p>  
    </div>
</div> 
{{> foot }} 